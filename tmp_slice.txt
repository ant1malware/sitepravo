<header className="sticky top-0 z-20 border-b border-zinc-200 bg-white/80 backdrop-blur supports-[backdrop-filter]:bg-white/60 dark:border-zinc-800 dark:bg-[color:var(--surface)]/90 dark:supports-[backdrop-filter]:bg-[color:var(--surface)]/70">
        <div className="mx-auto flex max-w-3xl items-center justify-between gap-3 px-4 py-3">
          <Link to="/" className="text-sm underline decoration-dotted hover:no-underline">На главную</Link>
          <h1 className="text-lg font-bold">Настройки</h1>
          {/* Accent custom and Backgrounds */}
          <section className="card">
            <h2 className="mb-2 text-base font-semibold">Акцент и фон</h2>
            <div className="mb-2 flex items-center gap-2 text-sm">
              <input type="color" value={accentCustom} onChange={(e)=>onAccentCustomChange(e.target.value)} title="Пользовательский акцент" />
              <span>Пользовательский цвет акцента</span>
            </div>
            <div className="mb-1 text-sm">Фон (только светлая тема):</div>
            <div className="flex flex-wrap gap-2 text-sm">
              {['none','bg1','bg2','bg3','custom'].map((id)=> (
                <button key={id} className={`btn ${bgSel===id?'btn-primary':''}`} onClick={()=>onBgSelect(id)}>
                  {id==='none'?'Нет':id==='custom'?'Свой':id.toUpperCase()}
                </button>
              ))}
            </div>
            <input className="input mt-2" placeholder="URL изображения для фона" onBlur={onBgCustomUrl} />
          </section>

        </div>
      </header>
